# Secretos y variables de entorno — NUNCA commitear
.env
.env.*
!.env.example

# Prompts internos de iteración (planes de trabajo puntuales, no parte del repo)
prompt-*.md

# Datos del mock de SAP
# Los .txt INVENTADOS de sap-mock/data/ son fixtures y SÍ se commitean.
# Cualquier dato real (export de SAP) va en sap-mock/data-real/ o sap-mock/private/, NUNCA en git.
sap-mock/data-real/
sap-mock/private/

# .NET (backend)
bin/
obj/
*.user
*.suo
*.userosscache
*.sln.docstates
.vs/

# SQLite local store (generated at runtime by the persistence adapter)
*.db
*.db-shm
*.db-wal

# Node / Next.js (frontend)
node_modules/
.next/
out/
dist/
build/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Editores e IDEs
.vscode/
.idea/
*.swp
*.swo

# Sistema
.DS_Store
Thumbs.db

# Logs
*.log
logs/
